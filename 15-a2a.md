# A2A：让多个 Agent 组成团队

> 一个 Agent 能力再强，也有边界。真正的复杂任务，需要多个 Agent 协作完成

---

前面十几篇，我们一直在打造"单兵"：给 Agent 装工具（MCP）、接 API（Plugin）、注入专业知识（Skill）。但现实中的复杂任务，往往不是一个 Agent 能搞定的。

比如"调研 A2A 协议的最新进展，写一篇技术对比文章，然后审校一遍"。这里面有三个角色：调研员、写手、审校员。让一个 Agent 全干，要么上下文爆炸，要么质量拉胯。

Google 在 2025 年 4 月提出了 **A2A（Agent-to-Agent）协议**，专门解决这个问题：让不同的 Agent 像团队成员一样发现彼此、分配任务、交换结果。这篇我们就来拆解 A2A 的核心设计，并用代码跑通整个流程。

---

## A2A 解决什么问题

在 A2A 之前，多 Agent 协作基本靠"硬编码"：你在代码里写死哪个 Agent 干什么，消息格式自己定，状态自己管。换一个 Agent 就得改一堆代码。

A2A 要做的事情很简单：**定义一套标准协议，让任意两个 Agent 之间能互相发现、互相通信、互相协作**。

它和 MCP 的关系经常被问到，一句话说清楚：

- **MCP** 解决的是 Agent 和工具之间的通信（Agent ↔ Tool）
- **A2A** 解决的是 Agent 和 Agent 之间的通信（Agent ↔ Agent）

MCP 让 Agent 能"用工具"，A2A 让 Agent 能"找队友"。两者互补，不冲突。

---

## A2A 的三个核心概念

A2A 协议的设计围绕三个核心概念展开：**Agent Card**、**Task**、**Message/Artifact**。理解了这三个，协议就掌握了八成。

### Agent Card：Agent 的"名片"

每个 Agent 都需要对外发布一张 **Agent Card**，声明自己是谁、能干什么、怎么联系。就像你在 LinkedIn 上的个人资料，别人看了就知道要不要找你合作。

Agent Card 是一个 JSON 文件，托管在 Agent 的 `/.well-known/agent.json` 路径下。核心字段：

```typescript
// agent-card.ts 节选

interface AgentCard {
  name: string                    // Agent 名称
  description: string             // 一句话介绍
  url: string                     // Agent 的服务地址
  version: string                 // 版本号
  capabilities: AgentCapabilities // 能力声明（是否支持流式、推送等）
  skills: AgentSkill[]            // 技能列表
  defaultInputModes: string[]     // 支持的输入格式
  defaultOutputModes: string[]    // 支持的输出格式
}
```

其中 `skills` 是最关键的部分。每个 Skill 有 `id`、`name`、`description`、`tags`，Orchestrator 就是靠这些信息来判断"这个任务该派给谁"。

我们在代码里模拟了三个 Agent 的注册表：

```typescript
// agent-card.ts 节选

const AGENT_REGISTRY: AgentCard[] = [
  {
    name: 'Research Agent',
    description: 'Searches the web and summarizes information on any topic',
    url: 'http://localhost:8001',
    version: '1.0.0',
    capabilities: { streaming: true, pushNotifications: false },
    defaultInputModes: ['text/plain'],
    defaultOutputModes: ['text/plain'],
    skills: [
      {
        id: 'web-search',
        name: 'Web Search',
        description: 'Search the internet for up-to-date information',
        tags: ['search', 'research', 'information'],
        inputModes: ['text/plain'],
        outputModes: ['text/plain'],
      },
      // ...
    ],
  },
  // Code Agent, Data Agent...
]
```

三个 Agent 各有专长：Research Agent 擅长搜索和总结，Code Agent 擅长写代码和 Review，Data Agent 擅长数据分析。Orchestrator 拿到用户需求后，就去注册表里找最合适的 Agent。

### Agent 发现机制

有了 Agent Card，下一步就是"发现"。Orchestrator 怎么知道该找谁？

在真实的 A2A 实现中，发现机制可以是：
- 直接访问 `/.well-known/agent.json`（已知地址）
- 通过注册中心查询（类似服务发现）
- 基于关键词 / 标签匹配

我们用 LLM + Function Calling 实现了一个智能发现流程：Orchestrator 理解用户需求，调用 `discover_agents` 工具，按关键词和标签匹配注册表中的 Agent：

```typescript
// agent-card.ts 节选

// 模拟 Agent 发现过程：按关键词分词匹配名称、描述、技能标签
function discoverAgents(query: string, requiredTags?: string[]): AgentCard[] {
  const keywords = query.toLowerCase().split(/\W+/).filter(w => w.length > 2)
  return AGENT_REGISTRY.filter(agent => {
    const corpus = [
      agent.name, agent.description,
      ...agent.skills.flatMap(s => [s.name, s.description, ...s.tags]),
    ].join(' ').toLowerCase()
    const keywordMatch = keywords.some(kw => corpus.includes(kw))
    const tagMatch = !requiredTags?.length || requiredTags.some(rt =>
      agent.skills.some(skill => skill.tags.includes(rt.toLowerCase()))
    )
    return keywordMatch && tagMatch
  })
}
```

运行 `pnpm agent-card`，你会看到 Orchestrator 根据用户需求自动发现合适的 Agent，并给出编排方案。

### Task：A2A 通信的基本单位

A2A 中所有 Agent 之间的协作，都以 **Task** 为单位。Task 不是简单的"一次请求/响应"，而是有完整生命周期的对象，能反映真实协作中的各种情况：开始、进行中、需要补充信息、完成、失败、被取消。

Task 的核心结构：

```typescript
// task-lifecycle.ts 节选

type TaskState =
  | 'submitted'      // 已提交，等待处理
  | 'working'        // 正在处理
  | 'input-required' // 需要用户补充信息
  | 'completed'      // 成功完成
  | 'failed'         // 执行失败
  | 'canceled'       // 被取消

interface Task {
  id: string
  contextId: string
  status: {
    state: TaskState
    message?: Message
    timestamp: string
  }
  messages: Message[]      // 完整的对话历史
  artifacts: Artifact[]    // Agent 产出的工件
}
```

这里有两个关键设计值得注意：

**第一，状态机是显式的**。不像普通的 HTTP 请求只有"成功/失败"两种结局，A2A Task 承认了一个现实：Agent 可能在中途卡住，需要补充信息（`input-required`），用户回复后再继续。这正好对应了真实协作中"我需要更多上下文才能干"的场景。

**第二，Message 和 Artifact 分离**。`messages` 是过程性的对话（"你需要的信息是 XX"），`artifacts` 是最终产出的成果（一份报告、一段代码、一张图表）。前者是沟通，后者是交付物。

### Message 和 Artifact 的多模态设计

A2A 的 Message 和 Artifact 都由 **Part** 组成，Part 可以是文本、数据、文件等多种类型：

```typescript
// task-lifecycle.ts 节选

interface TextPart {
  type: 'text'
  text: string
}

interface DataPart {
  type: 'data'
  data: Record<string, unknown>
}

type Part = TextPart | DataPart
```

这样设计的好处是：一个 Artifact 可以同时包含人类可读的文本和机器可读的结构化数据。Agent 之间传 Artifact，下游 Agent 既能看懂内容，又能直接消费 JSON。

---

## Task 生命周期实战

光看类型还不够，跑一遍状态流转才有体感。`task-lifecycle.ts` 演示了三个典型场景。

### 场景 1：直接完成

最简单的路径：`submitted → working → completed`。Agent 拿到任务，信息足够，直接产出结果：

```typescript
// task-lifecycle.ts 节选

async function agentProcess(task: Task): Promise<void> {
  // submitted → working
  updateTaskState(task.id, 'working', 'Processing your request...')

  // 让 LLM 决定：直接产出，还是要更多信息？
  const response = await openai.chat.completions.create({
    model: MODELS.GPT5_CODEX,
    messages,
    tools: agentTools,
    tool_choice: 'required',
  })

  const toolCalls = response.choices[0].message.tool_calls ?? []
  const call = toolCalls[0]
  const args = JSON.parse(call.function.arguments)

  if (call.function.name === 'produce_result') {
    // working → completed，同时产出 Artifact
    addArtifact(task.id, 'result', [
      { type: 'text', text: args.content },
      { type: 'data', data: { summary: args.summary } },
    ])
    updateTaskState(task.id, 'completed', args.summary)
  }
  // ...
}
```

注意这里用了 Function Calling 让 LLM 自己决定是 `produce_result` 还是 `request_more_info`。这正是 Agent 自治性的体现：Agent 自己判断信息够不够，而不是硬编码流程。

### 场景 2：需要补充信息

当 Agent 觉得信息不够时，进入 `input-required` 状态，"挂起"任务等待用户补充：

```
submitted → working → input-required
                            ↓
                    （用户提供更多信息）
                            ↓
                       working → completed
```

代码里的演示：用户先说"帮我优化数据库查询"，Agent 反问"具体是什么查询？数据量多大？"，然后用户补充 SQL 和数据规模，Agent 再继续处理。这个流程在真实业务里非常常见，A2A 把它做成了协议级别的标准状态。

### 场景 3：任务取消

`canceled` 状态用于客户端主动放弃任务。比如用户发现这个任务花的时间太长，或者需求变了，可以随时取消正在 `working` 的任务。

运行 `pnpm task-lifecycle` 能看到完整的三种流转。这里有个关键设计：**终态（completed / failed / canceled）一旦进入就不可逆**。这保证了 Task 的一致性，下游消费者可以安全地认为"completed 的 Artifact 不会再变"。

---

## 多 Agent 协作实战

把前面两块拼起来，就是完整的多 Agent 协作。`multi-agent.ts` 演示了一个 Orchestrator 编排三个 Agent 完成"研究 + 写作 + 审校"的流水线。

### Orchestrator 模式

A2A 的协作模式不要求"对等"，最常见的是 **Orchestrator 模式**：一个中心化的协调者负责拆解任务、分发给专业 Agent、汇总结果。

整个流程分三步：

**第一步，拆解任务**。Orchestrator 用 LLM 理解用户需求，决定要委派给哪些 Agent，每个 Agent 做什么子任务，子任务之间是否有依赖：

```typescript
// multi-agent.ts 节选

const orchestratorTools: ChatCompletionTool[] = [
  {
    type: 'function',
    function: {
      name: 'delegate_tasks',
      description: 'Break down a complex task into sub-tasks and assign each to the most suitable agent.',
      parameters: {
        type: 'object',
        properties: {
          plan: {
            type: 'array',
            items: {
              type: 'object',
              properties: {
                step: { type: 'number', description: 'Step number (same number = parallel execution)' },
                agentName: { type: 'string' },
                task: { type: 'string' },
                dependsOn: {
                  type: 'array',
                  items: { type: 'number' },
                  description: 'Step numbers this task depends on',
                },
              },
              required: ['step', 'agentName', 'task'],
            },
          },
        },
        required: ['plan'],
      },
    },
  },
]
```

让 LLM 输出结构化的执行计划，每一步都明确指定 Agent、任务描述、依赖关系。

**第二步，按依赖关系执行**。同一 step 的任务并行执行，不同 step 串行，前置 step 的输出作为后续 step 的上下文：

```typescript
// multi-agent.ts 节选

for (const stepNum of sortedSteps) {
  const tasks = stepGroups.get(stepNum)!

  // 收集依赖的上下文
  const getContext = (task: SubTask): string | undefined => {
    if (!task.dependsOn?.length) return undefined
    return task.dependsOn
      .map(dep => results.get(dep))
      .filter(Boolean)
      .map(r => `[${r!.agentName}]: ${r!.output}`)
      .join('\n\n')
  }

  // 同一 step 内的任务并行执行
  const promises = tasks.map(async task => {
    const agent = AGENTS.find(a => a.name === task.agentName)
    const context = getContext(task)
    const result = await sendMessageToAgent(agent!, task.task, context)
    return { step: task.step, result }
  })

  const stepResults = await Promise.all(promises)
  // ...
}
```

这是一个简化版的 DAG 调度器：能并行的并行，有依赖的串行，足以应对绝大多数协作场景。

**第三步，汇总结果**。所有 Agent 干完活之后，Orchestrator 再用一次 LLM 调用，把分散的输出整合成给用户的最终回复。

### 跑一遍看效果

运行 `pnpm multi-agent`，输入"写一篇 A2A vs MCP 的技术对比文章"。你会看到：

```
[Orchestrator] Execution plan:
  Step 1: Research Agent — Research the key differences between A2A and MCP...
  Step 2: Writer Agent — Write a technical comparison... (depends on step 1)
  Step 3: Reviewer Agent — Review the comparison for accuracy... (depends on step 2)
```

LLM 自动识别出了三步流水线，每一步都依赖上一步的产出。然后三个 Agent 依次工作，最后 Orchestrator 汇总。整个过程没有任何硬编码的流程，全靠协议和 LLM 的理解能力驱动。

---

## 踩坑与最佳实践

### 1. tool_choice 不要硬编码 function name

按照中转站的已知行为，`tool_choice` 不支持 `{ type: 'function', function: { name } }` 这种精确指定，必须用 `'required'`。代码里所有强制工具调用的地方都用了 `tool_choice: 'required'`，然后通过检查 `tool_calls.length > 0` 来判断，而不是依赖 `finish_reason`。

### 2. Agent 之间传上下文要克制

多 Agent 协作里最容易踩的坑是"每个 Agent 都把全部历史塞过去"，结果上下文炸了 Token，质量还下降。正确做法是：**只传下游 Agent 真正需要的部分**。比如审校 Agent 只需要拿到写作 Agent 的成品，不需要看研究 Agent 的原始资料。代码里 `getContext` 函数严格按 `dependsOn` 收集，避免污染。

### 3. 失败要显式建模，不要静默吞掉

Agent 失败是常态：网络超时、模型抽风、参数错误都可能。`task-lifecycle.ts` 里给每个 Task 都设置了 `failed` 终态，`multi-agent.ts` 里 `sendMessageToAgent` 用 try/catch 把错误转成 `TaskResult`。Orchestrator 收到失败结果后，可以选择重试、降级或终止整条流水线，但绝不能假装没发生。

### 4. Agent Card 的 description 决定发现质量

Orchestrator 选 Agent 的依据是 `description` 和 `skills.tags`，写得越具体越准确。`description: 'Searches the web and summarizes information'` 比 `description: 'A useful agent'` 强一百倍。把 Agent Card 当成 Agent 的"简历"来写，而不是占位符。

### 5. 不要让 Orchestrator 自己干活

最常见的反模式：Orchestrator 拆完任务后，自己也下场写一段。这会把"协调者"和"执行者"角色搞混，导致流程难以追溯。让 Orchestrator 只做三件事：拆解、分发、汇总。需要执行就再注册一个专门的 Agent。

---

## 小结

- **A2A 的核心是协议化的多 Agent 协作**：Agent Card 解决"找谁"，Task 生命周期解决"怎么协作"，Message/Artifact 解决"传什么"
- **A2A 和 MCP 互补**：MCP 让 Agent 用工具，A2A 让 Agent 找队友。一个完整的 Agent 系统通常两者都用
- **Orchestrator 模式是落地多 Agent 协作的最简起点**：一个协调者 + 多个专业 Agent，配合任务依赖图，能搞定 80% 的复杂任务

理解了 A2A，你就具备了从"单 Agent 系统"升级到"Agent 团队"的能力。下一篇我们进入工程化阶段，看看怎么把 Agent 系统真正落地到生产环境。

---

**下一篇**：Agent 工程化：从 Demo 到生产环境

---

*配套代码：[github.com/RyanWeb31110/ai-engineer-series](https://github.com/RyanWeb31110/ai-engineer-series)*

*「AI 工程师实战」系列第 15 篇*

---

**多 Agent 协作这块，我自己平时也在 Claude、GPT、Gemini 之间来回切换，每个 AI 工具的"性格"差别其实挺大的，组队干活前得先摸清各自的脾气。如果你也在折腾 AI 编码助手，欢迎加我交流，不管你主力用哪家，能聊到一块去就行。**

**加我微信，备注「AI编程」，拉你进交流群：**

`[你的微信号]`
