# Prompt Engineering：和 LLM 说话的艺术

> 同样的问题，措辞不同，结果可能天壤之别。这不是玄学，是有规律可循的工程技能。

你有没有遇到过这种情况：把同一个需求发给 ChatGPT，朋友得到的答案比你好得多，你问他怎么做到的，他说「只是换了个问法」。

这背后的原因是：LLM 对输入极其敏感，它不像传统程序那样执行精确指令，而是在理解你的意图之后，从无数可能的续写里选一条输出。**你怎么问，决定了它从哪个方向去找答案。**

Prompt Engineering 就是系统地利用这个特性：通过设计输入，引导模型输出你想要的结果。这是一项可以学习、可以复用的工程技能。

---
  
## System Prompt：给模型设定一个工作语境

打开任何 AI 应用，背后都有一段你看不到的文字，叫做 **System Prompt**（系统提示词）。它在对话开始之前就告诉模型：你是谁、你在什么场景下工作、你应该遵守哪些规则。

### 没有 System Prompt 的默认状态

直接问模型「帮我写一个 Python 函数，计算列表中所有偶数的和」，你会得到一段代码加上完整的解释，包括函数说明、参数注释、使用示例。这是模型的「默认行为」，它把你当作一个可能需要帮助的初学者。

加上一句 System Prompt 之后效果就不一样了：

```typescript
// prompt-basics.ts 节选

// 无 System Prompt：模型会给代码 + 解释
const messages = [{ role: 'user', content: USER_QUESTION }]

// 有 System Prompt：指定角色和输出风格
const res = await chat(messages, {
  model: MODELS.CLAUDE_HAIKU,
  maxTokens: 300,
  temperature: 0,
  systemPrompt: '你是一个 Python 专家。用户提问时，直接给出简洁的代码，不加任何解释或说明。',
})
```

运行 `pnpm basics`，你会看到：没有 System Prompt 时输出十几行；有 System Prompt 时只有 3~4 行代码，干净直接。

### System Prompt 的三个核心作用

**角色定义**：告诉模型它扮演谁，比如「你是一个严格的代码审查员」或「你是一个面向非技术用户的客服」。角色直接影响措辞风格、回答深度、对错误的容忍度。

**输出约束**：限定格式、长度、语言。「回答必须在 3 句话以内」「只用中文回答」「只输出 JSON，不加任何说明」。

**背景注入**：告诉模型它在什么上下文里工作，比如公司业务规则、数据库 schema、当前用户的订阅套餐。这让模型的回答更加贴合实际场景。

一个工程化的 System Prompt 结构通常是这样的：

```
你是 [角色定义]。

工作场景：[上下文描述]

规则：
1. [约束 1]
2. [约束 2]
3. [约束 3]

输出格式：[格式说明]
```

---

## Zero-shot、One-shot、Few-shot：示例的力量

**Shot** 在 Prompt Engineering 里指「示例」。

- **Zero-shot**：不给示例，直接问
- **One-shot**：给 1 个示例
- **Few-shot**：给 2~5 个示例

为什么示例有效？因为 LLM 在海量文本上训练，它非常擅长「模式匹配」。你给它看几个例子，它就能推断出你想要的输出模式，然后套用到新的输入上。

### 对比一下

同样是情感分类任务，Zero-shot 和 Few-shot 的 Prompt 写法差别很大：

```typescript
// Zero-shot：直接描述任务
{
  role: 'user',
  content: `判断以下文本的情感倾向，只回答"正面"、"负面"或"中性"：\n${text}`
}

// Few-shot：先给几个示例，再问目标问题
{
  role: 'user',
  content: `判断以下文本的情感倾向，只回答"正面"、"负面"或"中性"。

示例：
文本：物流很慢，等了一周才到。→ 负面
文本：价格合理，性价比不错。→ 正面
文本：包装很普通，没什么特别。→ 中性

现在判断：
文本：${text}→`
}
```

实际跑下来，两者在这道简单题上结果差不多。但换成输出格式复杂、判断标准模糊的任务，Few-shot 的优势就明显了：**示例比文字描述更能传达你的意图**。

### Few-shot 的注意事项

示例的质量比数量重要。3 个精准的示例通常强过 10 个随手写的。示例之间要覆盖边界情况，不只是最典型的正面案例。示例数量超过 5~6 个之后边际收益递减，Token 消耗增加。

---

## Chain of Thought (CoT)：让模型把推理过程写出来

这是 2022 年 Google 的一篇论文里提出的技巧，现在已经成为标准做法。

原理很简单：**让模型在给出答案之前，先把推理步骤写出来**。

这不是为了展示，而是因为 LLM 是逐 Token 生成的，它生成推理过程的动作本身，就在帮它「算」这道题。之前生成的推理文字会进入 Context，影响后续的生成。

### 一个直观对比

看这道题：

```
一张纸对折 5 次，厚度超过 3mm 吗？（纸厚 0.1mm）
```

直接问模型（Zero-shot），它可能直接报一个数字，错的概率不低。

加上「请一步一步思考」之后（Zero-shot CoT），模型会先列出每次对折后的厚度：

```
第 1 次：0.1 × 2 = 0.2mm
第 2 次：0.2 × 2 = 0.4mm
第 3 次：0.4 × 2 = 0.8mm
第 4 次：0.8 × 2 = 1.6mm
第 5 次：1.6 × 2 = 3.2mm

第 5 次后厚度为 3.2mm，超过 3mm。答案是 5 次。
```

回答准确了。推理步骤是「思考」，不是「废话」。

```typescript
// chain-of-thought.ts 节选

// 直接回答
const messages = [{ role: 'user', content: problem }]

// CoT：只加一句话
const cotProblem = problem.replace(
  '直接给出答案。',
  '请一步一步思考，写出推理过程，最后给出答案。'
)
```

运行 `pnpm cot`，你会看到同样的问题，两种提问方式下准确率的差异。

### CoT 的三种常见写法

**Zero-shot CoT**：在问题后面加「Let's think step by step」或「请一步一步分析」，适合临时用。

**Few-shot CoT**：在示例里直接展示带推理过程的答案，模型会学习这个格式，质量更稳定。

**Structured CoT**：用 XML 标签或 Markdown 结构强制分离推理和结论：

```
请先在 <thinking> 标签内完成分析，然后在 <answer> 标签内给出最终答案。
```

这个写法在需要可靠提取最终答案的生产场景里很实用，方便程序解析。

---

## 角色扮演：通过改变「对话对象」调整输出风格

让模型扮演特定角色，是快速调整输出风格的有效手段。

```typescript
// prompt-basics.ts 节选

const personas = [
  {
    label: '面向小学生解释',
    system: '你在跟一个 10 岁的小学生聊天，用简单易懂的话解释技术概念，多用生活中的比喻。',
  },
  {
    label: '面向有经验的程序员',
    system: '你在和一个有 5 年工作经验的程序员聊天，直接说技术本质，不需要绕弯子。',
  },
]
```

同一个问题「什么是递归？」，面向小学生的解释会用「镜子对镜子」或「俄罗斯套娃」类比；面向程序员的解释会直接讲调用栈、基本情形和递归情形。

角色扮演不是让模型「装」，而是在给它设定一个一致的输出基准，让它知道从哪个角度、用什么词汇、假设读者掌握什么背景知识来回答。

---

## 踩坑与最佳实践

### 1. 约束越清晰，结果越稳定

模糊的指令会让模型「自由发挥」，每次结果都不一样。把你的期望拆成可以验证的条件。

不好：「写一段产品介绍」
好：「写 3 句话的产品介绍，面向 B 端采购决策者，突出成本节省，不使用感叹号」

### 2. 输出格式要明确指定

如果你打算解析模型的输出，就要在 Prompt 里明确格式，不要指望模型自己猜。

不好：「给我一份分析报告」
好：「用以下 JSON 格式输出：{"summary": "...", "risk_level": "low|medium|high", "action_items": [...]}」

结构化输出是一个专门的话题，下一篇文章会详细讲，包括如何用工具保证 JSON 100% 合法。

### 3. CoT 不是万能药

CoT 在推理类、计算类、逻辑判断类任务上效果显著。但对于简单的问答、分类、翻译，加 CoT 只是浪费 Token，还会增加延迟。用的时候要对照任务性质判断。

### 4. System Prompt 过长会稀释注意力

System Prompt 不是越长越好。把所有你能想到的规则都塞进去，反而会让模型在关键位置注意力不集中，就像上一篇提到的「Lost in the Middle」问题。

工程经验：把最重要的规则放在 System Prompt 的开头和结尾，中间部分尽量精简。

### 5. 不要用 Prompt 解决本该用代码解决的问题

有些人会写很长的 Prompt 来处理数据转换、格式规整这类逻辑确定的任务。这类工作更适合用代码做，然后把处理后的结果交给模型。

Prompt 擅长处理模糊性、语义理解、创意生成。规则确定的逻辑用代码更可靠、更便宜。

---

## 小结

- **System Prompt** 设定工作语境和输出规则，是生产级 AI 应用最重要的基础设施
- **Few-shot 示例** 通过展示「你要的输出长什么样」来对齐意图，效果比文字描述更直接
- **Chain of Thought** 让模型先推理再回答，在复杂逻辑和计算任务上可以大幅提升准确率

掌握这三件事，你就能覆盖 80% 的 Prompt 设计场景。剩下的技巧大多是这三者的组合和延伸。

---

**下一篇**：结构化输出：让 AI 的回答变成程序能读的数据

---

*「AI 工程师实战」系列第 02 篇*
