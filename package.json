{
  "name": "ai-engineer-series",
  "private": true,
  "version": "0.0.1",
  "description": "AI 工程师实战系列 — 配套代码仓库",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
